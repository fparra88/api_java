# Copia a /etc/pendientes-api/pendientes-api.env y ajusta.
# Solo root y el usuario del servicio deben leerlo:
#   sudo chmod 640 /etc/pendientes-api/pendientes-api.env
#   sudo chown root:pendientes /etc/pendientes-api/pendientes-api.env
DB_HOST=localhost
DB_PORT=3306
DB_NAME=fyc
DB_USER=pendientes
DB_PASSWORD=cambia_esto
API_PORT=8080
